var myVar = "my variable";

function myFunc(x: number, y: number) {
    return x + y;
}

//ES6 / 2015
let num = 2;
const PI = 3.14;

var numbers = [1, 2, 3];

numbers.map(function(n) {
    return n * 2;
});

numbers.map(n => n * 2);

class Mathh { //Mathh, because Math already exists as a library.
    sum(x: number, y: number) {
        return x + y;
    }
}

let s1 = "string test";

